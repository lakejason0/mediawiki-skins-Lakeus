<header class="mw-header">
	{{>Logo}}
	<div id="p-user-tools">
        {{>Notifications}}
		{{#data-portlets.data-user-interface-preferences}}{{>Portlet}}{{/data-portlets.data-user-interface-preferences}}
	    {{>PersonalMenu}}
	</div>
	{{>Search}}
	{{>Sidebar}}
</header>
{{>Notices}}
<main id="content" class="mw-body">
	<header class="content__header">
		{{>ContentIndicators}}
		{{>ContentHeading}}
		{{>ContentTagline}}
		<div class="content__header-tabs">
			{{>ContentNamespaces}}
			{{>ContentActions}}
		</div>
	</header>
	{{>ContentBody}}
</main>
{{>Footer}}
